


const modalReducer = (state = {}, action ) => {
    Object.freeze(state)
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default modalReducer;